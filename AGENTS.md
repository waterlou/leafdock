# Leafdock

This repo is **Leafdock** — a Docker-based service that hosts web apps on the user's NAS, managed via a REST API.

Before deploying, updating, moving, or deleting any app, read `skills/leafdock/SKILL.md` in this repo: it documents the API endpoints, app types (static / docker / docker-compose), subfolder support, and the recommended deploy workflows.
